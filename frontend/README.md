# Frontend (React + Vite)

Nutri Suddh storefront frontend.

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```

## Notes

- Uses Vite proxy for `/api` to `http://localhost:8787`.
- Main app entry: `src/main.tsx` and `src/App.tsx`.
